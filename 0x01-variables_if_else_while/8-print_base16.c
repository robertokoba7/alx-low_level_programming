#include <stdio.h>

/**
 * main - prints all the numbers of base 16
 * Return: always 0
 */
int main(void)
{

	int n;

	for (n = 10; n < 20; n++)
	{
		putchar(n);
	}
	for (n = 22; n < 28; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}

