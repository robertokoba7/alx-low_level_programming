#include <stdio.h>

/**
 * main - single digit numbers of base 10
 *
 * Return: Always 0
 */

int main(void)
{
	int k;

	for (k = 0; k < 10; k++)
	{
		printf("%d", k);
	}
	putchar('\n');
	return (0);
}
