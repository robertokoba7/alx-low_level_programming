#include "main.h"
/**
 * add - Adds two integers and returns then returns results
 *
 * @c: number one.
 * @d: number two.
 *
 * Return: Sum of number one and number two
 */
int add(int c, int d)
{
	return (c + d);
}
